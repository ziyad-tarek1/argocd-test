---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  type: NodePort  # Choose NodePort for external access
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080  # Adjust the node port as needed
